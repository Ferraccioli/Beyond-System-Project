import { Meta } from '@storybook/addon-docs/blocks';
import {
  Add, EditOutlined, DeleteOutline, SaveOutlined, Send, ContentCopy, Download, Upload, Close, Search,
  ArrowBack, ArrowForward, Menu, MoreVert, MoreHoriz, ExpandMore, ExpandLess, ChevronRight, ChevronLeft,
  FilterList, FilterAlt, Sort, Refresh,
  CheckCircleOutline, ErrorOutline, WarningAmberOutlined, InfoOutlined, HourglassEmpty,
  PersonOutlined, BusinessOutlined, EmailOutlined, PhoneOutlined, ChatBubbleOutlineRounded, CalendarTodayOutlined, NotificationsOutlined, AttachFileOutlined, LabelOutlined,
  SmartToyOutlined, PsychologyOutlined, AutoFixHigh, DataObject,
  BarChart, TableChartOutlined, DashboardOutlined, TrendingUp,
  LockOutlined, SettingsOutlined, HelpOutlineOutlined, LogoutOutlined, AccountCircleOutlined,
} from '@mui/icons-material';

<Meta title="Tokens/Iconografia" />

# Iconografia

O Beyond System usa **Material Icons** (`@mui/icons-material`). A convenção é usar a variante **Outlined** para ações e navegação — transmite leveza e consistência. Variantes **Filled** são reservadas para estados ativos/selecionados.

---

## Convenção de variantes

<div style={{ fontFamily: 'Poppins, sans-serif', display: 'flex', gap: 32, padding: '16px 0' }}>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
    <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
      <PersonOutlined style={{ fontSize: 24, color: '#696c71' }} />
      <span style={{ fontSize: 13, color: '#202123' }}>Outlined — estado padrão</span>
    </div>
    <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
      <PersonOutlined style={{ fontSize: 24, color: '#4378ff' }} />
      <span style={{ fontSize: 13, color: '#202123' }}>Outlined + cor da marca — hover / foco</span>
    </div>
    <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
      <AccountCircleOutlined style={{ fontSize: 24, color: '#4378ff', fill: '#4378ff' }} />
      <span style={{ fontSize: 13, color: '#202123' }}>Filled — estado ativo / selecionado</span>
    </div>
  </div>
</div>

---

## Tamanhos recomendados

<table style={{ width: '100%', borderCollapse: 'collapse', fontFamily: 'Poppins, sans-serif', fontSize: 14 }}>
  <thead>
    <tr style={{ background: '#f6f8fa', borderBottom: '1px solid #d9d9e0' }}>
      <th style={{ padding: '8px 12px', textAlign: 'left', fontWeight: 600 }}>Contexto</th>
      <th style={{ padding: '8px 12px', textAlign: 'left', fontWeight: 600 }}>Tamanho</th>
      <th style={{ padding: '8px 12px', textAlign: 'left', fontWeight: 600 }}>Prop MUI</th>
    </tr>
  </thead>
  <tbody>
    {[
      ['Ícone dentro de input / label',  '16px', 'sx={{ fontSize: 16 }}'],
      ['Botão com ícone (padrão)',        '18px', 'sx={{ fontSize: 18 }}'],
      ['Navegação / sidebar',            '20px', 'sx={{ fontSize: 20 }}'],
      ['Ícone decorativo / destaque',    '24px', 'sx={{ fontSize: 24 }} ou fontSize="medium"'],
      ['Empty state / ilustração',       '48px', 'sx={{ fontSize: 48 }}'],
    ].map(([ctx, size, prop], i) => (
      <tr key={ctx} style={{ borderBottom: '1px solid #eff1f4', background: i % 2 === 0 ? '#fff' : '#fafafa' }}>
        <td style={{ padding: '8px 12px', color: '#202123' }}>{ctx}</td>
        <td style={{ padding: '8px 12px', color: '#696c71' }}>{size}</td>
        <td style={{ padding: '8px 12px' }}><code style={{ fontSize: 11, color: '#4378ff' }}>{prop}</code></td>
      </tr>
    ))}
  </tbody>
</table>

---

## Catálogo por categoria

### Ações principais

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 8, fontFamily: 'Poppins, sans-serif' }}>
  {[
    [Add,              'Add',              'Criar / adicionar item'],
    [EditOutlined,     'EditOutlined',     'Editar registro'],
    [DeleteOutline,    'DeleteOutline',    'Excluir (ação destrutiva)'],
    [SaveOutlined,     'SaveOutlined',     'Salvar alterações'],
    [Send,             'Send',             'Enviar mensagem / email'],
    [ContentCopy,      'ContentCopy',      'Copiar conteúdo'],
    [Download,         'Download',         'Baixar arquivo'],
    [Upload,           'Upload',           'Subir arquivo'],
    [Close,            'Close',            'Fechar / cancelar'],
  ].map(([Icon, name, desc]) => (
    <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 10px', background: '#f6f8fa', borderRadius: 6 }}>
      <Icon style={{ fontSize: 20, color: '#4378ff', flexShrink: 0 }} />
      <div>
        <div style={{ fontSize: 11, fontWeight: 500, color: '#202123' }}>{name}</div>
        <div style={{ fontSize: 10, color: '#696c71' }}>{desc}</div>
      </div>
    </div>
  ))}
</div>

### Busca, filtro e navegação

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 8, fontFamily: 'Poppins, sans-serif', marginTop: 8 }}>
  {[
    [Search,       'Search',       'Campo de busca'],
    [FilterList,   'FilterList',   'Filtrar lista'],
    [FilterAlt,    'FilterAlt',    'Filtro aplicado'],
    [Sort,         'Sort',         'Ordenar coluna'],
    [Refresh,      'Refresh',      'Recarregar dados'],
    [ArrowBack,    'ArrowBack',    'Voltar'],
    [ArrowForward, 'ArrowForward', 'Avançar'],
    [ChevronRight, 'ChevronRight', 'Expandir / navegar'],
    [ChevronLeft,  'ChevronLeft',  'Recolher / voltar'],
    [ExpandMore,   'ExpandMore',   'Abrir dropdown'],
    [ExpandLess,   'ExpandLess',   'Fechar dropdown'],
    [MoreVert,     'MoreVert',     'Menu de ações (kebab)'],
    [MoreHoriz,    'MoreHoriz',    'Mais opções (inline)'],
    [Menu,         'Menu',         'Abrir sidebar / nav'],
  ].map(([Icon, name, desc]) => (
    <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 10px', background: '#f6f8fa', borderRadius: 6 }}>
      <Icon style={{ fontSize: 20, color: '#696c71', flexShrink: 0 }} />
      <div>
        <div style={{ fontSize: 11, fontWeight: 500, color: '#202123' }}>{name}</div>
        <div style={{ fontSize: 10, color: '#696c71' }}>{desc}</div>
      </div>
    </div>
  ))}
</div>

### Status e feedback

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 8, fontFamily: 'Poppins, sans-serif', marginTop: 8 }}>
  {[
    [CheckCircleOutline,      'CheckCircleOutline',      '#24923c', 'Sucesso / confirmado'],
    [ErrorOutline,            'ErrorOutline',            '#fc0303', 'Erro / falha'],
    [WarningAmberOutlined,    'WarningAmberOutlined',    '#c79200', 'Aviso / atenção'],
    [InfoOutlined,            'InfoOutlined',            '#4378ff', 'Informação'],
    [HourglassEmpty,          'HourglassEmpty',          '#94979b', 'Aguardando / processando'],
  ].map(([Icon, name, color, desc]) => (
    <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 10px', background: '#f6f8fa', borderRadius: 6 }}>
      <Icon style={{ fontSize: 20, color, flexShrink: 0 }} />
      <div>
        <div style={{ fontSize: 11, fontWeight: 500, color: '#202123' }}>{name}</div>
        <div style={{ fontSize: 10, color: '#696c71' }}>{desc}</div>
      </div>
    </div>
  ))}
</div>

### Comunicação e contatos

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 8, fontFamily: 'Poppins, sans-serif', marginTop: 8 }}>
  {[
    [PersonOutlined,              'PersonOutlined',              'Contato / pessoa'],
    [BusinessOutlined,            'BusinessOutlined',            'Empresa / organização'],
    [EmailOutlined,               'EmailOutlined',               'Email'],
    [PhoneOutlined,               'PhoneOutlined',               'Telefone'],
    [ChatBubbleOutlineRounded,    'ChatBubbleOutlineRounded',    'Mensagem / chat'],
    [CalendarTodayOutlined,       'CalendarTodayOutlined',       'Data / agendamento'],
    [NotificationsOutlined,       'NotificationsOutlined',       'Notificação'],
    [AttachFileOutlined,          'AttachFileOutlined',          'Anexo'],
    [LabelOutlined,               'LabelOutlined',               'Tag / etiqueta'],
  ].map(([Icon, name, desc]) => (
    <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 10px', background: '#f6f8fa', borderRadius: 6 }}>
      <Icon style={{ fontSize: 20, color: '#696c71', flexShrink: 0 }} />
      <div>
        <div style={{ fontSize: 11, fontWeight: 500, color: '#202123' }}>{name}</div>
        <div style={{ fontSize: 10, color: '#696c71' }}>{desc}</div>
      </div>
    </div>
  ))}
</div>

### Agentes e IA

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 8, fontFamily: 'Poppins, sans-serif', marginTop: 8 }}>
  {[
    [SmartToyOutlined,   'SmartToyOutlined',  '#a745ff', 'Agente / bot'],
    [PsychologyOutlined, 'PsychologyOutlined','#a745ff', 'IA / inteligência'],
    [AutoFixHigh,        'AutoFixHigh',       '#a745ff', 'Automação / geração'],
    [DataObject,         'DataObject',        '#696c71', 'Dados estruturados / JSON'],
  ].map(([Icon, name, color, desc]) => (
    <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 10px', background: '#f6ecff', borderRadius: 6 }}>
      <Icon style={{ fontSize: 20, color, flexShrink: 0 }} />
      <div>
        <div style={{ fontSize: 11, fontWeight: 500, color: '#202123' }}>{name}</div>
        <div style={{ fontSize: 10, color: '#696c71' }}>{desc}</div>
      </div>
    </div>
  ))}
</div>

### Dados e analytics

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 8, fontFamily: 'Poppins, sans-serif', marginTop: 8 }}>
  {[
    [BarChart,            'BarChart',            'Gráfico de barras'],
    [TrendingUp,          'TrendingUp',          'Tendência / crescimento'],
    [TableChartOutlined,  'TableChartOutlined',  'Tabela de dados'],
    [DashboardOutlined,   'DashboardOutlined',   'Dashboard / visão geral'],
  ].map(([Icon, name, desc]) => (
    <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 10px', background: '#f6f8fa', borderRadius: 6 }}>
      <Icon style={{ fontSize: 20, color: '#3264e2', flexShrink: 0 }} />
      <div>
        <div style={{ fontSize: 11, fontWeight: 500, color: '#202123' }}>{name}</div>
        <div style={{ fontSize: 10, color: '#696c71' }}>{desc}</div>
      </div>
    </div>
  ))}
</div>

### Sistema e conta

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))', gap: 8, fontFamily: 'Poppins, sans-serif', marginTop: 8 }}>
  {[
    [SettingsOutlined,       'SettingsOutlined',       'Configurações'],
    [LockOutlined,           'LockOutlined',           'Permissão / acesso'],
    [HelpOutlineOutlined,    'HelpOutlineOutlined',    'Ajuda / documentação'],
    [AccountCircleOutlined,  'AccountCircleOutlined',  'Perfil do usuário'],
    [LogoutOutlined,         'LogoutOutlined',         'Sair da conta'],
  ].map(([Icon, name, desc]) => (
    <div key={name} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '8px 10px', background: '#f6f8fa', borderRadius: 6 }}>
      <Icon style={{ fontSize: 20, color: '#696c71', flexShrink: 0 }} />
      <div>
        <div style={{ fontSize: 11, fontWeight: 500, color: '#202123' }}>{name}</div>
        <div style={{ fontSize: 10, color: '#696c71' }}>{desc}</div>
      </div>
    </div>
  ))}
</div>
